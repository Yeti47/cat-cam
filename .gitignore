.venv/
__pycache__/
*.pyc
*.pt
snapshots/*.jpg
!snapshots/.gitkeep
ntfy-server/cache/
cat_cam.egg-info/

# Local secrets: app-level (ntfy topic) and compose-level (Netbird IP,
# device paths). Templates are tracked as *.example instead.
config.local.yaml
.env
